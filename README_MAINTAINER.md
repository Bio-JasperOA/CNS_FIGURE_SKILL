# Maintainer notes

The Skill remains major version 3 and `spec_version: '3.0'`. v3.2 adds 26-family table-driven style rendering without changing the existing palette IDs. All demo data must remain marked synthetic. No subtitle is rendered.
